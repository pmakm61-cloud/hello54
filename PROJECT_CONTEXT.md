## 🏷️ ПРОЕКТ: PARSER-CRM (hello54) — АКТУАЛЬНАЯ СТРУКТУРА

### 📁 ФАКТИЧЕСКАЯ СТРУКТУРА В ГИТЕ:
hello54/ # КОРЕНЬ ПРОЕКТА (парсер здесь)
├── crm/ # Отдельный модуль CRM (FastAPI)
│ ├── app.py
│ └── templates/index.html
├── src/ # ИСХОДНЫЙ КОД ПАРСЕРА (основной модуль)
│ ├── config.py # Конфиг PostgreSQL
│ ├── database.py # Подключение к БД
│ └── ... # Основная логика парсера
├── data/ # Данные парсинга (JSON/Excel)
├── requirements.txt # ЕДИНЫЙ файл зависимостей (в корне)
├── .env.example
├── .gitignore
└── README.md


### 🎯 КЛЮЧЕВОЙ НЮАНС:
- **Парсер** — находится в **корне проекта** (`src/`, `data/`, основные скрипты)
- **CRM** — отдельная папка `crm/` на том же уровне
- **НЕТ папки `parser/`** — весь код парсера в корневых директориях

### ⚙️ ТЕХНОЛОГИИ:
- **Python 3.11+**, **FastAPI** (CRM), **SQLAlchemy**, **PostgreSQL**
- **Парсинг**: aiohttp, BeautifulSoup4, Selenium, pandas (работает!)
- **Конфигурация БД**: `src/config.py` + `.env` файлы

### ✅ ЧТО РАБОТАЕТ:
1. ✅ **Парсер** — собирает данные, подключен к PostgreSQL
2. ✅ **БД** — конфигурация настроена в `src/config.py`
3. ✅ **CRM-каркас** — FastAPI приложение запускается
4. ✅ **Зависимости** — единый `requirements.txt` в корне

### 🔄 ЛОГИКА ПРОЕКТА:

Парсер (корень/scr/) → PostgreSQL → CRM (папка /crm/)
↓
data/ (результаты)


### ⚠️ ВАЖНО ДЛЯ БУДУЩЕЙ РАБОТЫ:
- **Импорты из парсера** в CRM: `from src.config import ...`
- **Запуск парсера** — из корня проекта
- **Запуск CRM** — из папки `crm/` или через относительные импорты

### 📝 ПОСЛЕДНИЕ ИЗМЕНЕНИЯ:
начали разработку crm модуля 

### 🎯 ЗАДАЧА НА ЭТОТ ДИАЛОГ:
Интеграция парсера с CRM через API

### ❓ ВОПРОСЫ:
Создай и дополни структуру crm для соответствующей папки проекта, и дай рабочий прототип файлов для быстрого отображения данных из бд
